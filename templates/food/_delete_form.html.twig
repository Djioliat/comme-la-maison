<form method="post" action="{{ path('food_delete', {'id': food.id}) }}"
      onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce plat?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ food.id) }}">
    <button class="w3-button w3-red w3-half">Supprimer</button>
</form>
