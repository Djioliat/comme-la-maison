{% extends 'admin.html.twig' %}

{% block title %}Edit Food
{% endblock %}

{% block body %}
	<section class="w3-container w3-center w3-margin-bottom">
		<h1>Editer "{{ food.name }}"</h1>
	</section>
	<section class="w3-container w3-third w3-center w3-margin-bottom w3-padding">
		{{ include('food/show.html.twig') }}
	</section>
	<section class="w3-container w3-margin-top w3-rest w3-center">
		{{ include('food/_form.html.twig', {'button_label': 'Update'}) }}
	</section>
	<section class="w3-container w3-center">
		<a class="w3-button w3-border w3-hover-light-gray w3-xlarge w3-margin-bottom" href="{{ path('food_index') }}">Retour
			            à la liste</a>
	</section>
{% endblock %}
