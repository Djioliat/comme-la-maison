<form method="post" action="{{ path('wine_delete', {'id': wine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wine.id) }}">
    <button class="w3-button w3-red mt-5 ml-4">Supprimer</button>
</form>